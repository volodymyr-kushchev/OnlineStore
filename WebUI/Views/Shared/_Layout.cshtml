<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="~/Content/bootstrap-grid.min.css" rel="stylesheet" />
    <link href="~/Content/bootstrap-reboot.min.css" rel="stylesheet" />
    <link href="~/Content/bootstrap.min.css" rel="stylesheet" />
    <link href="~/Content/Site.css" rel="stylesheet" />
</head>
<body>
    <div class="navbar navbar-inverse" role="navigation">
        @* tried show all books deserialized from Json. status: doesn't work*@
        @*@using (Ajax.BeginForm("ShowBooks", "Admin", new AjaxOptions { OnSuccess = "OnSuccess" }))
        {
            <input type="submit" value="All Books" />
        }*@
        <div class="navbar-right hidden-xs">
            @Html.ActionLink("Корзина", "Index", "Cart", null,
            new { @class = "btn btn-default navbar-btn" })
        </div>
    </div>

    <div class="row panel">
        <div id="genres" class="col-sm-3 hidden-xs">
            @Html.Action("Categories", "Nav")
        </div>
        <div class="col-xs-12 col-sm-8">
            @RenderBody()
        </div>
    </div>
</body>
</html>